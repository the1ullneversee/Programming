// Dynamic_memory_allocation.cpp : Defines the entry point for the console application.
//<summary> Create an application that uses Dynamic Memory allocation to let the user enter in any amount of numbers they want, then any number of names</summary>

#include "stdafx.h"
#include <stdio.h>
#include <iostream>
#include <string>
int main()
{
	//Step 1: Declare variables that will hold the user input of element amount.
	//Step 2: Ask user and gather their input.
	//Step 3: Using the new operator, dynamically allocate an array and assign it to a pointer
	//Step 4: Loop through array and get the user to input data into it
	//Step 5: Let the user see what they have inputted into the container. 
	//Step 6: Repeat for array of strings. 
    return 0;
}

